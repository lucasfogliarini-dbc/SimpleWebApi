apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: simple-workflow
  labels:
    app.kubernetes.io/name: simple-workflow
    app.kubernetes.io/part-of: simple-workflow-project
    kubernetes.azure.com/generator: devhub
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: simple-workflow
  minReplicas: 1
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 80